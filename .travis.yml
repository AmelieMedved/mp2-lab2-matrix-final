language: cpp
compiler:
  - gcc
  - clang
before_script:
  - mkdir ../build
  - cd ../build
script:
  - cmake ../mp2-lab2-matrix
  - make -j
  - ls 
  - ./test/MP2-TESTS
notifications:
  email: false
